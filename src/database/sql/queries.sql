-- NOTE: SELECT Queries

-- name: getUsers :many
SELECT * in users;

-- name: projectSelect :many
SELECT * in projects;

-- name: taskSelect :many
SELECT * in tasks;

-- TODO: {USER} Add account creation, task creation and project creation INSERT queries
